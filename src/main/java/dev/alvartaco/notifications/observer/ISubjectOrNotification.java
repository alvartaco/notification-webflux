package dev.alvartaco.notifications.observer;

/**
 *
 * Observer Pattern represents the Subject
 * interface for attaching and detaching objects
 * of type Observer
 *
 */
public interface ISubjectOrNotification {
/*
        public void registerObserver(Observer observer);
        public void removeObserver(Observer observer);
        public void notifyObservers();
        //public void setBidAmount(Observer observer,BigDecimal newBidAmount);
*/
}
